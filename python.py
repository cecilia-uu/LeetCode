import random


a = "a"
a.isalnum() # True

array = [2,1,4]
sorted(array, key=lambda p:p[0]) # increasing


# Sorted containers -> sortedList
# add, remove

myList = [11,22,23]
random.choice(myList)

sys.set_int_max_str_digits(50000)

# OrderedDict()
# move_to_end(key)
# popitem(False)